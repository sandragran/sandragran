# Site settings
title: Sandra Gran
description: Marine Biologist & Data Scientist — coastal monitoring, phytoplankton ecology & ocean data
# URL of the live site (no trailing slash)
url: "https://sandragran.github.io"

# baseurl should be empty for user/organization pages
baseurl: ""

# Theme (Cayman) via remote_theme
remote_theme: pages-themes/cayman@v0.2.0
plugins:
  - jekyll-remote-theme


# Markdown settings
markdown: kramdown
kramdown:
  input: GFM

# Build settings
permalink: /:path/
paginate: 0

# Exclude from the build (optional)
exclude:
  - node_modules
  - vendor
  - Gemfile
  - Gemfile.lock

# Optional: social links for templates (not required)
author:
  name: "Sandra Gran"
  email: "sandra.granstad@gmail.com"
